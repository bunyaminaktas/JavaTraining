package hw07;

abstract interface Interface{
	/*
	 * - All interface methods are assumed to have public.
	 * 
	 * - Abstract interface methods exist to be overridden in the classes that implements the interface
	 * so that they cannot be marked with modifiers which prevents method from being overridden such as final.
	 * 
	 * - Interfaces can have static methods but static modifier cannot be used with abstract modifier.
	 */
	//protected abstract void methodA();
	//static abstract void methodB();
	//final abstract void methodC();
	public abstract void methodD();
}

public class Q02 {

}
