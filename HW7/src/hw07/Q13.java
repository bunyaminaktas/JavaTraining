package hw07;

interface Test{
	/*
	 * - All interface methods are assumed to have public.
	 * 
	 * - Interface methods exist to be overridden in the classes that implements the interface
	 * so that they cannot be marked with modifiers which prevents method from being overridden such as final.
	 * 
	 * - Interfaces can have static methods and they must have a body.
	 */
	//protected void methodA();
	static void methodB() {
		
	}
	//final void methodC();
	//private void methodD();

}

public class Q13 {

}
