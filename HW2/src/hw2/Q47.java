package hw2;

// Class name had to be changed to Q47
public class Q47 {
	/*
	 * Constructors must have same name with the class.
	 * Constructors must not have a return type.
	 */
	
	// This is the only valid way of defining a constructor. It also may have parameters.
	public Q47() {
		
	}
	
	
	/*
	 * Constructors does not have return types
	 */
	//public TennisBall static create { return new TennisBall(); }
	
	/*
	 * Constructor does not have return types. This method also contain a colon (:) which has no use in Java.
	 */
	// public TennisBall static newInstance() { return new TennisBall(): }
	
	/*
	 * This is a normal method which has same name with the class. It is not recommended but allowed.
	 */
	// public void TennisBall() {}
}
