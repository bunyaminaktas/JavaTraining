package hw2;

public class Q6 {
	
	//public String convert(int value) { return value.toString();	} // int is not a class. So it cannot use toString() method.
	public String convert(Integer value) { return value.toString(); }
	public String convert(Object value) { return value.toString(); }
}
