package hw2;

public class Q49 {
	Integer in = new Integer(5); // Wrapper classes takes primitives as parameters in their constructors 
									// and converts them to wrapper objects. 
	
	/*
	 * These methods do not exist.
	 */
	//Integer in2 = Integer.asObject(5);
	//Integer in3 = Integer.convertToObject(5);
	//Integer in4 = Integer.toObject(5);
}
