package counting;
import java.util.*;

// Class name had to be changed to Q40
public class Q40 {
	public static void main(String... args) {
		/*
		 * - Since no argument is passed to the program, the array args is initialized empty
		 */
		
		Arrays.sort(args);
		System.out.println(Arrays.toString(args)); 
	}
}
