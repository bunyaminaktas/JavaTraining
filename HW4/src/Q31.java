// Class name had to be changed to Q31
public class Q31 {
	public static void main(String target[]) {
		/*
		 * - Since two arguments is passed to the program the parameter array of the main() method
		 * will be initialized as 2 sized, which causes the program output 2.
		 */
		System.out.println(target.length);
	}
}
