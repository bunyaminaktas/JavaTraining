// Class name had to be changed to Q44
public class Q44 {
	public static void main(String[] names) {
		/*
		 * - Since no arguments is passed to the program, an empty array will be declared.
		 * And since the array is zero sized, the next statement will cause an ArrayIndexOutOfBoundsException.
		 */
		
		System.out.println(names[0]);
	}
}
