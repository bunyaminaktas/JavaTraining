// Class name had to be changed to Q38
public class Q38 {
	public static void main(String target[]) {
		System.out.println(target.length);
		
		// Original code. There is no length() method so the code does not compile.
		//System.out.println(target.length());
	}
}
